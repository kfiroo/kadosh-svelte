{
    "name": "kadosh-svelte",
    "version": "1.0.0",
    "devDependencies": {
        "rollup": "^1.12.0",
        "rollup-plugin-commonjs": "^10.0.0",
        "rollup-plugin-livereload": "^1.0.0",
        "rollup-plugin-node-resolve": "^5.2.0",
        "rollup-plugin-svelte": "^5.0.3",
        "rollup-plugin-terser": "^5.1.2",
        "svelte": "^3.0.0",
        "jest": "^24.9.0"
    },
    "dependencies": {
        "lodash": "^4.17.15",
        "sirv-cli": "^0.4.4",
        "svelte-dnd": "^0.0.10"
    },
    "scripts": {
        "build": "rollup -c",
        "dev": "rollup -c -w",
        "start": "sirv public --single",
        "start:dev": "sirv public --single --dev",
        "test": "jest",
        "surge": "surge --project ./public --domain kadosh.surge.sh --add yotamb@wix.com,yotambarzilay@gmail.com",
        "deploy": "yarn build && yarn surge"
    },
    "git": {
        "scripts": {
            "pre-push": "yarn deploy"
        }
    }
}
